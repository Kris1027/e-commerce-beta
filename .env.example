# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/ecommerce_db?schema=public"

# For uses requiring a connection without pgbouncer (migrations, etc.)
DATABASE_URL_UNPOOLED="postgresql://user:password@localhost:5432/ecommerce_db?schema=public"

# Parameters for constructing your own connection string
PGHOST="example-host.com"
PGHOST_UNPOOLED="example-host.com"

# Authentication (NextAuth v5)
# In production, AUTH_URL should be set to your production URL
AUTH_URL=http://localhost:3000
AUTH_SECRET=your-auth-secret-here-min-32-chars

# UploadThing
# Get your token from: https://uploadthing.com/dashboard
# After creating an app in UploadThing dashboard
UPLOADTHING_TOKEN=sk_live_...
